server:
  port: 8080

notion:
  api_token: "${NOTION_API_TOKEN}"
  database_id: "${NOTION_DATABASE_ID}"

discord:
  webhook_url: "${DISCORD_WEBHOOK_URL}"

notification:
  days_before: 3
  check_schedule: "0 12 * * *"  # 毎日正午12時 (JST)
